/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["sir-app.developerconsole.xyz","ui-avatars.com"]
    },
}

module.exports = nextConfig
